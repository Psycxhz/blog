```
定义 $符号
插值
父选择器 &:hover{}
!optional防止编译失败
指令
#{}字符串模板
@import 导入sass文件并合并 并且调用变量
@extend 继承复用样式 @extend 加上类名
%命名{}选择器编译符
if()就是三木运算符 
@if @else
@for $let from <start>through <start> <end>循环
@for $let from <start>to <start>
@while 


```

